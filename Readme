
# Install the CodeDeploy Agent
sudo yum update -y
sudo yum install -y ruby
sudo yum install -y wget
cd /home/ec2-user
wget https://aws-codedeploy-us-west-2.s3.amazonaws.com/latest/install
chmod +x ./install
sudo ./install auto
sudo service codedeploy-agent start


#Set Up IAM Role for CodeDeploy:

Ensure that your EC2 instance has an IAM role attached with the AWSCodeDeployRole policy.


#########################################################################################################################3
step 1
1 if yuo docrize the image use
# Use an official Python runtime as a parent image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /app

# Copy the current directory contents into the container at /app
COPY . /app

# Expose port 8080 to the outside world
EXPOSE 8080

# Run simple_web_server.py when the container launches
CMD ["python", "simple_web_server.py"]


step2
 Set Up the AWS CodeDeploy Configuration
appspec.yml
 version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/simple-web-server

hooks:
  AfterInstall:
    - location: scripts/start_server.sh
      timeout: 300
      runas: ec2-user



##########################################################3
Create the Start Script

#!/bin/bash

# Stop any existing Docker containers running the web server
docker stop simple-web-server || true
docker rm simple-web-server || true

# Pull the latest Docker image
docker pull your-docker-repo/simple-web-server:latest

# Run the Docker container
docker run -d -p 8080:8080 --name simple-web-server your-docker-repo/simple-web-server:latest


